https://drive.google.com/file/d/1UTRiYjivUpvl2xg2A1cAzMg7WuA7LOQ5/view?usp=sharing (Figma Model)
### 1. `AndroidManifest.xml`

Make sure to declare activities and permissions:

```xml
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.citizenapp">

    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.CitizenApp">
        <activity android:name=".InputOptionsActivity"/>
        <activity android:name=".DashboardActivity"/>
        <activity android:name=".LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
    </application>
</manifest>
```

---

### 2. `LoginActivity.java`

```java
package com.example.citizenapp;

import android.content.Intent;
import android.os.Bundle;
import android.view.View;
import android.widget.Button;
import android.widget.EditText;
import androidx.appcompat.app.AppCompatActivity;

public class LoginActivity extends AppCompatActivity {

    EditText otpInput;
    Button verifyBtn;

    @Override
    protected void onCreate(Bundle savedInstanceState) {
        super.onCreate(savedInstanceState);
        setContentView(R.layout.activity_login);

        otpInput = findViewById(R.id.otpInput);
        verifyBtn = findViewById(R.id.verifyBtn);

        verifyBtn.setOnClickListener(new View.OnClickListener() {
            @Override
            public void onClick(View v) {
                String otp = otpInput.getText().toString().trim();
                if (otp.length() == 4) { // Simple check
                    startActivity(new Intent(LoginActivity.this, DashboardActivity.class));
                    finish();
                } else {
                    otpInput.setError("Enter valid OTP");
                }
            }
        });
    }
}
```

**`activity_login.xml`**

```xml
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:orientation="vertical"
    android:gravity="center"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="20dp">

    <TextView
        android:text="OTP Login"
        android:textSize="24sp"
        android:textStyle="bold"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"/>

    <EditText
        android:id="@+id/otpInput"
        android:hint="Enter OTP"
        android:inputType="number"
        android:maxLength="4"
        android:layout_marginTop="20dp"
        android:gravity="center"
        android:layout_width="200dp"
        android:layout_height="wrap_content"/>

    <Button
        android:id="@+id/verifyBtn"
        android:text="Verify"
        android:layout_marginTop="20dp"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"/>
</LinearLayout>
```

---

### 3. `DashboardActivity.java`

```java
package com.example.citizenapp;

import android.content.Intent;
import android.os.Bundle;
import android.view.View;
import android.widget.Button;
import androidx.appcompat.app.AppCompatActivity;

public class DashboardActivity extends AppCompatActivity {

    Button reportBtn, trackBtn;

    @Override
    protected void onCreate(Bundle savedInstanceState) {
        super.onCreate(savedInstanceState);
        setContentView(R.layout.activity_dashboard);

        reportBtn = findViewById(R.id.reportBtn);
        trackBtn = findViewById(R.id.trackBtn);

        reportBtn.setOnClickListener(new View.OnClickListener() {
            @Override
            public void onClick(View v) {
                startActivity(new Intent(DashboardActivity.this, InputOptionsActivity.class));
            }
        });

        trackBtn.setOnClickListener(new View.OnClickListener() {
            @Override
            public void onClick(View v) {
                // For now just show InputOptions too
                startActivity(new Intent(DashboardActivity.this, InputOptionsActivity.class));
            }
        });
    }
}
```

**`activity_dashboard.xml`**

```xml
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:orientation="vertical"
    android:gravity="center"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <TextView
        android:text="Dashboard"
        android:textSize="24sp"
        android:textStyle="bold"
        android:layout_marginBottom="30dp"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"/>

    <Button
        android:id="@+id/reportBtn"
        android:text="Report an issue"
        android:backgroundTint="@android:color/holo_red_light"
        android:layout_width="200dp"
        android:layout_height="wrap_content"/>

    <Button
        android:id="@+id/trackBtn"
        android:text="Track your issue"
        android:layout_marginTop="20dp"
        android:backgroundTint="@android:color/holo_orange_light"
        android:layout_width="200dp"
        android:layout_height="wrap_content"/>
</LinearLayout>
```


4. `InputOptionsActivity.java`

java
package com.example.citizenapp;

import android.os.Bundle;
import android.view.View;
import android.widget.Button;
import android.widget.Toast;
import androidx.appcompat.app.AppCompatActivity;

public class InputOptionsActivity extends AppCompatActivity {

    Button cameraBtn, micBtn, locationBtn, uploadBtn;

    @Override
    protected void onCreate(Bundle savedInstanceState) {
        super.onCreate(savedInstanceState);
        setContentView(R.layout.activity_input_options);

        cameraBtn = findViewById(R.id.cameraBtn);
        micBtn = findViewById(R.id.micBtn);
        locationBtn = findViewById(R.id.locationBtn);
        uploadBtn = findViewById(R.id.uploadBtn);

        cameraBtn.setOnClickListener(v -> Toast.makeText(this, "Open Camera", Toast.LENGTH_SHORT).show());
        micBtn.setOnClickListener(v -> Toast.makeText(this, "Start Voice Input", Toast.LENGTH_SHORT).show());
        locationBtn.setOnClickListener(v -> Toast.makeText(this, "Add Location", Toast.LENGTH_SHORT).show());
        uploadBtn.setOnClickListener(v -> Toast.makeText(this, "Upload File", Toast.LENGTH_SHORT).show());
    }
}
```

**`activity_input_options.xml`**

xml
<GridLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:rowCount="2"
    android:columnCount="2"
    android:gravity="center"
    android:padding="20dp">

    <Button
        android:id="@+id/cameraBtn"
        android:text="Click Picture"
        android:layout_row="0"
        android:layout_column="0"
        android:layout_width="140dp"
        android:layout_height="140dp"/>

    <Button
        android:id="@+id/micBtn"
        android:text="Voice Input"
        android:layout_row="0"
        android:layout_column="1"
        android:layout_width="140dp"
        android:layout_height="140dp"/>

    <Button
        android:id="@+id/locationBtn"
        android:text="Add Location"
        android:layout_row="1"
        android:layout_column="0"
        android:layout_width="140dp"
        android:layout_height="140dp"/>

    <Button
        android:id="@+id/uploadBtn"
        android:text="Upload"
        android:layout_row="1"
        android:layout_column="1"
        android:layout_width="140dp"
        android:layout_height="140dp"/>
</GridLayout>
